version: '0.0.{build}'

branches:
  only:
    - master

environment:
  matrix:
    - compiler: msvc-16-seh
      generator: "Visual Studio 16 2019"
      build_system: cmake
      APPVEYOR_BUILD_WORKER_IMAGE: Visual Studio 2019

    - compiler: msvc-15-seh
      generator: "Visual Studio 15 2017"
      build_system: cmake
      APPVEYOR_BUILD_WORKER_IMAGE: Visual Studio 2017

    - compiler: msvc-14-seh
      generator: "Visual Studio 14 2015"
      build_system: cmake
      APPVEYOR_BUILD_WORKER_IMAGE: Visual Studio 2015

configuration:
  - Debug

before_build:
    - ci\before_build.sh

build_script:
    - ci\build_script.sh

test_script:
    - ci\test_script.sh

after_test:
    - ps: .\ci\after_test.ps1
# THIS WORKS
# after_test:
#     - ps: (new-object net.webclient).UploadFile("https://ci.appveyor.com/api/testresults/junit/$($env:APPVEYOR_JOB_ID)", (Resolve-Path .\TestRunner_results.xml))
# THIS WORKS
# before_build:
#     - cmake -H. -B./build
#
# build_script:
#     - cmake --build build
#
# test_script:
#     - build\test\bin\TestRunner.exe
